说明：

1.在网上找了大量例子、教程，按照留言板所需功能完成编写，每天做一点，今天终于完成了。
采用课堂上讲的php写的，最后建立新的数据库并测试。

2.webhomework.sql里有一些测试数据，测试时不用大量操作，直接先导入数据库
然后进入浏览器访问http://localhost/index.html按要求进入留言板即可，方便武老师检查。

3.数据库主机名，密码，建立新的数据库（更改数据库名字即可）等可以在config.php文件中改写。
